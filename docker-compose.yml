services:
  mysql:
    image: ${MYSQL_IMAGE:-mysql:8.0.36}
    environment:
      MYSQL_DATABASE: wordpress
      MYSQL_USER: wp
      MYSQL_PASSWORD: wp
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "${MYSQL_PORT:-3306}:3306"
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10

  wordpress:
    image: ${WORDPRESS_IMAGE:-wordpress:7.0.1-php8.2-apache}
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      WORDPRESS_DB_HOST: mysql
      WORDPRESS_DB_USER: wp
      WORDPRESS_DB_PASSWORD: wp
      WORDPRESS_DB_NAME: wordpress
      WORDPRESS_DEBUG: "true"
      WORDPRESS_DEBUG_LOG: /var/www/html/wp-content/debug.log
    ports:
      - "${WORDPRESS_PORT:-80}:80"
    volumes:
      - ./:/var/www/html/wp-content/plugins/webmastery-site-toolkit-for-mcp
      - wp_data:/var/www/html
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/wp-json/"]
      timeout: 20s
      retries: 10

volumes:
  wp_data:
